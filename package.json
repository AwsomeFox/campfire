{
  "name": "campfire",
  "private": true,
  "version": "0.2.0",
  "description": "Self-hosted, AI-operable D&D campaign tracker",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n server,web -c yellow,cyan \"npm run dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/schema && npm run build -w apps/server && npm run build -w apps/web",
    "test": "npm run test -w apps/server",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.6.3"
  }
}
