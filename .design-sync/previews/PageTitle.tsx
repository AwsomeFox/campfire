import { PageTitle } from '@campfire/web';

export const Sizes = () => (
  <div style={{ display: 'grid', gap: 20, padding: 16 }}>
    <PageTitle>The Sunken Crown</PageTitle>
    <PageTitle size="compact">Session 14 — Into the Deep</PageTitle>
  </div>
);
